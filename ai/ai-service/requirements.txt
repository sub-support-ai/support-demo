fastapi>=0.135,<1
uvicorn[standard]>=0.42,<1
python-dotenv>=1.2,<2
requests>=2.32,<3
# httpx нужен для FastAPI TestClient (starlette.testclient импортирует его).
# Используется только в тестах, но лежит в основных зависимостях, чтобы
# тест-среда не требовала отдельного requirements-dev.txt.
httpx>=0.27,<1
