fastapi>=0.135,<1
# python-multipart нужен FastAPI для OAuth2PasswordRequestForm в /auth/login.
# В старых версиях FastAPI тянулся транзитивно, с 0.95+ стал опциональным —
# без него импорт app падает с RuntimeError ещё на старте (эндпоинт с формой
# проверяет наличие multipart в момент регистрации роута).
# Минимальная версия 0.0.18 — там пофикшен CVE-2024-53981 (DoS через
# длинный boundary в multipart-заголовке).
python-multipart>=0.0.18,<1
uvicorn[standard]>=0.42,<1
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.31,<1
pydantic[email]>=2.12,<3
# pydantic-settings 2.x — backend для класса Settings (BaseSettings).
# До этого Settings собирался вручную через os.getenv(); миграция дала
# нам типобезопасность через field_validator'ы и SecretStr для секретов
# (JWT_SECRET_KEY, AI_SERVICE_API_KEY) — теперь они не уезжают в repr/log.
pydantic-settings>=2.5,<3
python-dotenv>=1.2,<2
bcrypt>=5,<6
email-validator>=2.3,<3
httpx>=0.28,<1
sentry-sdk[fastapi]>=2.0,<3
python-jose[cryptography]>=3.3,<4
alembic>=1.14,<2
# Redis — необязательный backend для rate-limiter (RATE_LIMIT_BACKEND=redis).
# Нужен, когда backend крутится в нескольких uvicorn-воркерах / pod'ах:
# in-memory-счётчики у каждого инстанса свои, и общий лимит «5 попыток в
# минуту с IP» превращается в «5 × N инстансов» — брутфорс не остановить.
redis>=5,<6
# PyYAML — нужен импортёру KB из Markdown-файлов с YAML frontmatter
# (scripts/import_knowledge_from_markdown.py). Confluence-export и docs-as-code
# в Git — типичные источники таких файлов у заказчиков.
PyYAML>=6.0,<7
# Prometheus-метрики: HTTP-инструментация (request_total, latency) и
# кастомные gauges/histograms для очередей и времени обработки джоб.
# /metrics endpoint закрывается на уровне nginx/ingress в production.
prometheus-fastapi-instrumentator>=7,<8
prometheus-client>=0.22,<1